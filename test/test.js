const {expect} = require('chai')

describe('Scan Latters', () => {

  it.skip('has latters', (next) => {

    const {URL} = require('url')
    const http = require('http')
    const cheerio = require('cheerio')

  })
})